# fix density

mu <- 0.4
sigma <- 0.2

ggplot() +
  geom_point(
    data = tibble(
      x = mu,
      y = dnorm(mu, mean = mu, sd = sigma)
    ),
    aes(x = x, y = y)
  ) +
  stat_function(fun = dnorm, args = list(mean = mu, sd = sigma)) +
  ylim(0, 2.5) +
  xlim(0, 1) +
  NULL
